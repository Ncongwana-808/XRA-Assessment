# Simple Time Service

This is a simple Python web service that returns the current date and time based on the server's local timezone.

## Requirements

- Python 3.x
- Flask (`pip install flask`)

## How to Run

1. Clone or download the repository containing `time_service.py`.
2. Create and activate a virtual environment:
   python -m venv venv
   venv\Scripts\activate       # On Windows